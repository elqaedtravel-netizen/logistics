import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary & Navy Dark Palette (Enterprise Logistics SaaS)
  static const Color primaryDark = Color(0xFF070F2B);      // Deepest Navy
  static const Color primary = Color(0xFF1B1A55);          // Royal Navy Brand
  static const Color primaryLight = Color(0xFF535C91);     // Medium Indigo Accent
  static const Color brandSecondary = Color(0xFF00B4D8);   // Vibrant Logistics Cyan
  static const Color secondary = Color(0xFF0F766E);        // Teal Slate

  // Vibrant Egyptian Accents & Highlights
  static const Color accent = Color(0xFFFF6500);           // High-contrast Action Orange
  static const Color accentLight = Color(0xFFFF8A08);      // Amber Glow
  static const Color accentMuted = Color(0xFFFFF3E0);      // Light Orange Tint

  // Clean Surface & Backgrounds
  static const Color background = Color(0xFFF8FAFC);      // Crisp Slate Gray-50
  static const Color surface = Color(0xFFFFFFFF);         // Pure White
  static const Color surfaceElevated = Color(0xFFF1F5F9); // Light Gray-100
  static const Color border = Color(0xFFE2E8F0);          // Neutral Gray-200
  static const Color borderFocused = Color(0xFF3B82F6);   // Active Border

  // Sidebar & Navigation Specifics
  static const Color sidebarBg = Color(0xFF0B132B);       // Dark Obsidian
  static const Color sidebarActive = Color(0xFF1C2541);   // Active Tab
  static const Color sidebarHover = Color(0xFF151E3F);    // Hover Tab

  // Typography Tokens
  static const Color textPrimary = Color(0xFF0F172A);     // Slate 900
  static const Color textSecondary = Color(0xFF475569);   // Slate 600
  static const Color textMuted = Color(0xFF94A3B8);       // Slate 400
  static const Color textWhite = Color(0xFFFFFFFF);

  // Status Indicators (Bosta / Aramex Colors)
  static const Color statusPending = Color(0xFFD97706);      // Amber (قيد الانتظار)
  static const Color statusInWarehouse = Color(0xFF6366F1);  // Indigo (في المخزن)
  static const Color statusDispatched = Color(0xFF0284C7);   // Sky Blue (مع المندوب)
  static const Color statusDelivered = Color(0xFF059669);    // Emerald Green (تم التسليم)
  static const Color statusPostponed = Color(0xFFEA580C);    // Burnt Orange (مؤجل)
  static const Color statusCanceled = Color(0xFFDC2626);     // Rose Red (ملغي)
  static const Color statusReturned = Color(0xFF7C3AED);     // Deep Violet (مرتجع)

  // Payment Logos & Cards
  static const Color vodafoneRed = Color(0xFFE60000);
  static const Color meezaGreen = Color(0xFF007A3D);
  static const Color instapayPurple = Color(0xFF6C27B0);
}
